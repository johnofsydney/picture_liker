    <nav class="w-full flex flex-row justify-between" style="display: block">
      <div>
        <% if notice.present? %>
          <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
        <% end %>
      </div>
      <div class="buttonsinside">
        <%= link_to "Home", root_path, class: "py-2 px-3 bg-blue-600 text-white font-medium rounded-lg" %>
        <% if current_user %>
          <%= link_to "Sign out", destroy_user_session_path, 'data-turbo-method': :delete, class: "py-2 px-3 bg-red-600 text-white font-medium rounded-lg" %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "py-2 px-3 bg-blue-600 text-white font-medium rounded-lg" %>
        <% end %>
      </div>
    </nav>