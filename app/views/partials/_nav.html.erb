<nav class="container w-full flex flex-row justify-between items-center mb-5">
  <div>
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>
    <% if current_admin_user && @user %>
      <p class="py-2 px-3 bg-green-50 text-green-500 font-medium rounded-lg inline-block" id="notice">Results for <%= @user.email %></p>
    <% end %>
    <p>TODO: Branding, TurboStream, Add name to User, Default URL for Mailer, Menu, Worker for Uploads, Rollbar</p>
    <p><%= ActionMailer::Base.default_url_options[:host] %></p>
  </div>
  <div class="buttonsinside">
    <% if current_user %>
      <%= link_to "Sign out", destroy_user_session_path, 'data-turbo-method': :delete, class: "py-2 px-3 bg-red-600 text-white font-medium rounded-lg" %>
    <% elsif current_admin_user %>
      <%= link_to "Admin User Sign out", destroy_admin_user_session_path, 'data-turbo-method': :delete, class: "py-2 px-3 bg-red-600 text-white font-medium rounded-lg" %>
      <%= link_to "Admin User Dashboard", '/admin', class: "py-2 px-3 bg-blue-600 text-white font-medium rounded-lg" %>
    <% elsif !%w[sessions passwords].include?(controller_name) %>
      <%= link_to "Sign in", new_user_session_path, class: "py-2 px-3 bg-blue-600 text-white font-medium rounded-lg" %>
    <% else %>

    <% end %>
  </div>
</nav>